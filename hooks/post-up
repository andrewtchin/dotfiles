# Install vim bundles
vim +BundleInstall! +BundleClean +qall

VIMPROC_DIR="$HOME/.vim/bundle/vimproc"
if [ -d "$VIMPROC_DIR" ]; then
    echo "Building vimproc."
    cd $VIMPROC_DIR; make; cd -
fi

YCM_DIR="$HOME/.vim/bundle/YouCompleteMe"
if [ -d "$YCM_DIR" ]; then
    echo "Building YCM."
    cd $YCM_DIR
    ./install.sh --clang-completer
    cd -
fi
